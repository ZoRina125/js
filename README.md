# Лабораторная работа №2. Реализация базовых методов работы с массивами

## Цель работы

Целью данной работы является изучение принципов реализации базовых методов работы с массивами с использованием колбэков: `forEach`, `map`, `filter`, `find`, `some`, `every`, `reduce`, а также закрепление навыков написания функций с корректным возвратом результатов и обработкой граничных случаев.



### 1.1. Вывод массива в консоль

Реализуем функцию `printArray(array)`, которая принимает массив и *выводит его элементы в консоль* (`console.log`).
<img width="530" height="238" alt="image" src="https://github.com/user-attachments/assets/85db04d3-d3a4-4a15-b191-c5263af9f51d" />



<img width="284" height="82" alt="image" src="https://github.com/user-attachments/assets/23f9e227-4deb-4c2b-8118-31ea57d8aee4" />


Теперь, реализуем функцию `printArray1(array)`, которая выводит элементы массива в консоль в виде:
<img width="563" height="105" alt="image" src="https://github.com/user-attachments/assets/6de869c0-3dff-4da0-8a80-a566b8a45859" />


<img width="499" height="286" alt="image" src="https://github.com/user-attachments/assets/52ba5c38-e3e1-44f3-9990-fc6451487b02" />




Как можно видеть, для каждого формата вывода требуется своя функция. Это связано с тем, что форматирование вывода может быть разным в зависимости от контекста использования функции. Например, в одном случае может потребоваться более подробное описание элемента (как в `printArray`), а в другом - более краткое (как в `printArray1`). 

### 1.2. Функция `forEach(array, callback)`

Функция выполняет переданный колбэк для каждого элемента массива.

_Требования_:

- Функция принимает два аргумента:
  - `array` - массив
  - `callback` - функция обратного вызова
- Для каждого элемента массива вызывает переданный колбэк.
  - Колбэк должен принимать три аргумента: текущий элемент, индекс элемента и весь массив.

    ```js
    callback(element, index, array);
    ```

- Функция ничего не возвращает (возвращаемое значение - `undefined`).
- Обход массива должен происходить слева направо.

<img width="593" height="450" alt="image" src="https://github.com/user-attachments/assets/a5b0d1de-b852-4db6-973c-de749647acfd" />

<img width="544" height="111" alt="image" src="https://github.com/user-attachments/assets/f238b376-cc98-4589-a373-91abe54c4bcc" />


### 2. Функция `map(array, callback)`

Функция создает новый массив, _содержащий результаты вызова колбэка для каждого элемента исходного массива_. Простыми словами, `map` преобразует каждый элемент массива в новый элемент на основе логики, определенной в колбэке.

_Требования_:

- Функция принимает два аргумента:
  - `array` - массив
  - `callback` - функция обратного вызова
- Для каждого элемента массива вызывает переданный колбэк.
  - Колбэк должен принимать три аргумента: текущий элемент, индекс элемента и весь массив.

    ```js
    callback(element, index, array);
    ```

  - _Второй и третий аргументы являются необязательными_. Если колбэк принимает только один аргумент, он должен работать корректно.

- Функция возвращает новый массив, содержащий результаты вызова колбэка для каждого элемента.
- Длина результирующего массива должна совпадать с длиной исходного.

<img width="724" height="597" alt="image" src="https://github.com/user-attachments/assets/fecc8991-566d-4d43-97ca-bcdfe4f356ea" />

<img width="551" height="68" alt="image" src="https://github.com/user-attachments/assets/277d9ac4-a732-4f8b-a79d-fab037a2fdc2" />


### 3. Функция `filter(array, callback)`

Функция формирует новый массив из элементов, удовлетворяющих условию, заданному в колбэке. Простыми словами, `filter` позволяет отфильтровать элементы массива на основе логики, определенной в колбэке.


_Требования_:

- Функция принимает два аргумента:
  - `array` - массив
  - `callback` - функция обратного вызова
- Для каждого элемента массива вызывает переданный колбэк.
  - Колбэк должен принимать три аргумента: текущий элемент, индекс элемента и весь массив.

    ```js
    callback(element, index, array);
    ```

  - _Второй и третий аргументы являются необязательными_. Если колбэк принимает только один аргумент, он должен работать корректно.
  - Колбэк должен возвращать `true`, если элемент удовлетворяет условию, и `false` в противном случае.

- Функция возвращает новый массив, содержащий все элементы, для которых колбэк вернул значение `true`.
- Длина результирующего массива может быть меньше или равна длине исходного.
<img width="799" height="668" alt="image" src="https://github.com/user-attachments/assets/7663a7f0-dc4f-4399-96af-06c2d11cafcc" />

<img width="593" height="66" alt="image" src="https://github.com/user-attachments/assets/a251b57a-bf31-41f7-99c9-5a83b4374102" />

### 4. Функция `find(array, callback)`

Функция возвращает первый элемент массива, удовлетворяющий условию.

_Требования_:

- Функция принимает два аргумента:
  - `array` - массив
  - `callback` - функция обратного вызова
- Для каждого элемента исходного массива (`array`) вызывает переданный колбэк.
  - Колбэк должен принимать три аргумента: текущий элемент, индекс элемента и весь массив.

    ```js
    callback(element, index, array);
    ```

  - _Второй и третий аргументы являются необязательными_. Если колбэк принимает только один аргумент, он должен работать корректно.

  - Колбэк должен возвращать `true`, если элемент удовлетворяет условию, и `false` в противном случае.

- Функция возвращает первый элемент, для которого колбэк вернул значение `true`. Если такой элемент не найден, возвращает `undefined`.

<img width="694" height="617" alt="image" src="https://github.com/user-attachments/assets/cbde2e0c-7b83-4932-baec-d273e52a63a5" />



### 5. Функция `some(array, callback)`

Функция проверяет, существует ли хотя бы один элемент массива, удовлетворяющий условию.



_Требования_:

- Принимает два аргумента:
  - `array` - массив
  - `callback` - функция обратного вызова
- Для каждого элемента массива вызывает переданный колбэк.
  - Колбэк должен принимать три аргумента: текущий элемент, индекс элемента и весь массив.

    ```js
    callback(element, index, array);
    ```

  - _Второй и третий аргументы являются необязательными_. Если колбэк принимает только один аргумент, он должен работать корректно.
  - Колбэк должен возвращать `true`, если элемент удовлетворяет условию, и `false` в противном случае.

- Функция возвращает `true`, если колбэк вернул значение `true` для любого элемента массива, и `false` в противном случае.
- Обход массива должен происходить слева направо и прекращаться при первом совпадении.

<img width="723" height="595" alt="image" src="https://github.com/user-attachments/assets/f20f1008-653c-4443-9cf8-1289c8be36da" />


### 6. Функция `every(array, callback)`

Функция проверяет, удовлетворяют ли все элементы массива заданному условию.


_Требования_:

- Принимает два аргумента:
  - `array` - массив
  - `callback` - функция обратного вызова
- Для каждого элемента массива вызывает переданный колбэк.
  - Колбэк должен принимать три аргумента: текущий элемент, индекс элемента и весь массив.

    ```js
    callback(element, index, array);
    ```

  - _Второй и третий аргументы являются необязательными_. Если колбэк принимает только один аргумент, он должен работать корректно.
  - Колбэк должен возвращать `true`, если элемент удовлетворяет условию, и `false` в противном случае.

- Возвращает `true`, если колбэк вернул значение `true` для всех элементов массива, и `false` в противном случае.
- Обход массива должен происходить слева направо и прекращаться при первом несоответствии.

<img width="827" height="624" alt="image" src="https://github.com/user-attachments/assets/29d1c2ad-d226-4f3d-ac05-973aacea43cb" />


### (_Дополнительно_) 7. Функция `reduce(array, callback, initialValue)`

Функция последовательно обрабатывает элементы массива, накапливая результат в аккумуляторе.

_Требования_:

- Принимает три аргумента:
  - `array` - массив
  - `callback` - функция обратного вызова. Колбэк принимает четыре аргумента:
  - `initialValue` - начальное значение аккумулятора

- Для каждого элемента массива вызывает переданный колбэк.
  - Колбэк должен принимать четыре аргумента: аккумулятор, текущий элемент, индекс элемента и весь массив.

    ```js
    callback(accumulator, element, index, array);
    ```

  - _Третий и четвертый аргументы являются необязательными_. Если колбэк принимает только два аргумента, он должен работать корректно.
  - Колбэк должен возвращать новое значение аккумулятора после обработки текущего элемента.
  - Если `initialValue` не предоставлено, первый элемент массива используется в качестве начального значения аккумулятора, а обработка начинается со второго элемента.
  - Если `initialValue` предоставлено, аккумулятор инициализируется этим значением, а обработка начинается с первого элемента массива.
  - Если массив пустой и `initialValue` не предоставлено, функция должна вернуть `undefined`.

- Функция возвращает итоговое значение аккумулятора после обработки всех элементов массива.
- Обход массива должен происходить слева направо.
- После завершения обхода вернуть итоговое значение аккумулятора.
<img width="918" height="255" alt="image" src="https://github.com/user-attachments/assets/1cb6ead8-37ec-47e2-a1e1-526081d142d5" />

<img width="851" height="698" alt="image" src="https://github.com/user-attachments/assets/d83a8fd6-a0a1-4544-bc1f-b0ce9c91f32a" />

<img width="791" height="159" alt="image" src="https://github.com/user-attachments/assets/ce576912-506a-4d60-a5d8-f90a4860e4dd" />


<img width="249" height="44" alt="image" src="https://github.com/user-attachments/assets/3d26f245-1ab2-47f8-a5c6-b9db3f52f871" />




## Контрольные вопросы

1. В чем преимущества использования колбэков при работе с массивами
   
При работе с массивами использование колбэков дает мне прежде всего декларативный стиль написания кода. Вместо того чтобы вручную управлять индексами, счетчиками и условиями выхода в цикле for, я просто передаю методу "инструкцию" в виде функции. Это делает код намного чище и понятнее для других разработчиков: когда видишь filter, сразу понимаешь, что идет отбор элементов, а map сигнализирует о трансформации данных. Кроме того, это повышает безопасность, так как исключаются типичные ошибки вроде выхода за пределы массива или бесконечных циклов.

2. Какие проблемы могут возникать при использовании колбэков и как их избежать?
   
Однако у колбэков есть и свои минусы, главный из которых — это проблема читаемости при глубокой вложенности. Когда одна асинхронная операция зависит от другой, код начинает некрасиво расти вправо, и в нем становится трудно ориентироваться. Чтобы этого избежать, можно переходить на промисы или синтаксис async/await. Еще одна частая проблема — потеря контекста this. Если передать метод объекта как обычный колбэк, он может забыть, к какому объекту относится. Эту проблему я решаю использованием стрелочных функций, которые автоматически сохраняют контекст родителя.

4. Как реализовать функции `map`, `filter`, `find`, `some`, `every` и `reduce` без использования встроенных методов массивов?объектов?

Если нужно реализовать эти методы самостоятельно, то под капотом всё равно будет лежать обычный цикл. Например, для создания своего map я создаю новый пустой массив, прохожу циклом по исходному и на каждой итерации вызываю колбэк, результат которого пушу в новый массив. В случае с filter логика похожая, но в новый массив элемент попадает только в том случае, если колбэк вернул true. Методы some и every работают как переключатели: some возвращает true, как только нашелся подходящий элемент, а every возвращает false, если хотя бы один элемент не прошел проверку. Для find я просто прерываю цикл и возвращаю текущий элемент, как только условие выполнилось.

Реализация reduce чуть сложнее, так как там появляется понятие аккумулятора — переменной, которая копит результат на каждом шаге и передается в следующую итерацию. Если же нужно применить эти методы к объектам, я сначала перевожу их в итерируемый вид. Обычно я использую Object.keys, Object.values или Object.entries, чтобы получить массив ключей или значений, а затем уже применяю к этому массиву всю вышеописанную логику через циклы. Такой подход позволяет работать с данными любой структуры, соблюдая единый стандарт обработки.
